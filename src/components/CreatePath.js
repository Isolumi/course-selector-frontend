const CreatePath = () => {
    return ( 
        <div className="createPath">
            <h2>Create a new path</h2>
            <form onSubmit={handleSubmit}>
                
            </form>
        </div>
     );
}
 
export default CreatePath;